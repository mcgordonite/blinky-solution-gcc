{
    "tasks": [
        {
            "type": "cmsis-project-manager.build",
            "project": "${command:cmsis-project-manager.getProject}",
            "problemMatcher": [],
            "label": "Build",
            "group": "build"
        }
    ]
}
